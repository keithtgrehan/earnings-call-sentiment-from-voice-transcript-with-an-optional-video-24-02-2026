# Optional multimodal sidecars only.
# The base repo works without these packages.
#
# WhisperX adds word-level alignment and optional speaker-aware enrichment.
# pyannote.audio adds optional speaker diarization support.
#
# FinBERT and emotion-english-distilroberta-base reuse the base repo's
# existing transformers/torch dependencies and are configured by model id.
#
# OpenFace is intentionally not listed here because it is usually installed
# as a separate system-level tool rather than a Python package in this repo.

whisperx>=3.8,<4
pyannote-audio>=4,<5
