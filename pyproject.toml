[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "earnings-call-sentiment"
version = "0.1.0"
description = "Earnings call sentiment from voice transcript with optional video input."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Keith" }]
license = { text = "MIT" }

# Move runtime deps from requirements.txt into here.
# (If you paste your requirements.txt content, I’ll fill this exactly.)
dependencies = [
  # "typer>=0.9",
  # "rich>=13",
  # "yt-dlp>=2024.0.0",
  # "faster-whisper>=1.0.0",
  # "soundfile>=0.12",
]

[project.scripts]
earnings-call-sentiment = "app.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
app = ["py.typed"]
