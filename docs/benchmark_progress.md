# Benchmark Progress

## Current state
- Planned benchmark calls: 9
- Completed raw transcripts: 9
- Pending raw transcripts: 0
- Draft label rows prepared: 9

## Strongest current benchmark candidates
- `call08` (LLY): explicit raised-guidance sentence in the transcript, currently the best `raised` example
- `call09` (PVH): explicit lowered-guidance sentence in the transcript for full-year non-GAAP metrics, currently the best `lowered` example
- `call02` (LEU): explicit maintenance language (`our guidance is flat`)
- `call01` (PLTR): explicit forward guidance range, useful `unclear` example
- `call03` (GOOGL): official IR upload, useful `unclear` control

## Current gaps
- The benchmark now contains one `raised`, one `lowered`, one `maintained`, and six `unclear` rows.
- Most directional rows still rely on repost video sources, so source-quality remains a review consideration even though the transcript evidence is explicit.
- `call02` / LEU remains the messiest metadata row and should still be handled with care.
